line-length = 120
target-version = "py313"
extend-exclude = [
  ".venv",
  ".tmp",
  "build",
  "dist",
  "debugCapture",
  "local_resources",
  "resources/live2d_samples",
  "resources/spine_samples",
]

[lint]
select = ["E", "F", "I"]
ignore = [
  "E501",
]
